# v06：召回质量阈值过滤

## 版本目标

通过 minScore 过滤低质量召回片段，避免弱相关内容进入大模型上下文。

## 修复前

系统只按 Top-K 控制数量。即使后几条结果相关性很弱，也会为了凑满数量被送给大模型。

## 修复后

在 Rerank 后增加最低分阈值，低于阈值的内容直接过滤。上下文可以少，但不能乱。

## 产品价值

v05 让参考资料顺序更可信，v06 进一步控制参考资料质量，减少无关上下文对生成结果的污染。
