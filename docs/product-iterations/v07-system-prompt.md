# v07：生成提示词约束

## 版本目标

通过系统提示词提升回答正确性，减少模型脱离上下文自由发挥。

## 修复前

即使检索结果正确，模型也可能补充上下文里没有的信息，导致回答看起来流畅但不够可靠。

## 修复后

系统提示词明确要求模型基于上下文回答，信息不足时说明不足，不强行编造。

## 产品价值

v06 控制输入上下文质量，v07 控制模型怎么使用上下文。一个管材料，一个管表达。
