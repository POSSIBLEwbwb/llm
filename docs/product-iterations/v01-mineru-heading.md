# v01：文档解析结构化

## 版本目标

把 PDF 解析后的 Markdown 结构变得更适合 RAG 入库。

## 修复前

MinerU 能把 PDF 转成 Markdown，但多级标题不稳定。标题层级丢失后，系统很难按章节切分，容易把不同主题混在一个 chunk 里。

## 修复后

在文档预处理阶段补充标题层级识别，让章节结构更清楚，后续可以按标题做更自然的分段。

## 产品价值

这是 RAG 产品的地基。只有文档结构稳定，后面的切分、召回和引用才有可靠基础。
