# v09：生成模型忠实度优化

## 版本目标

通过更换生成模型，解决回答忠诚度低的问题。

## 修复前

模型语言能力不错，但在 RAG 场景中容易加入上下文之外的推测，Faithfulness 指标偏低。

## 修复后

对比不同模型在上下文忠实度、中文表达和业务稳定性上的表现，选择更适合 RAG 的模型。

## 产品价值

v08 保证评测标准可靠，v09 再根据评测结果选择模型。这里体现的是数据驱动调优，而不是凭感觉换模型。
