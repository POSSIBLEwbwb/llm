# v04：多路检索融合稳定化

## 版本目标

解决 RRF 融合后结果重复、顺序不稳定的问题。

## 修复前

向量检索和关键词检索都可能召回同一个 chunk，但默认融合逻辑不按业务 `chunkId` 判断相等，导致同一片段重复出现，RRF 分数也没有正确累加。

## 修复后

按 `chunkId` 做业务级去重和分数累加，再按融合分数排序，最后交给 Reranker 精排。

## 产品价值

v03 保证 Query 干净，v04 让多个检索器的结果真正融合起来，而不是简单堆在一起。Top-K 的信息密度会更高。
