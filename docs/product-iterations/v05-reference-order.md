# v05：引用资料顺序修复

## 版本目标

解决引用信息补全后，参考资料顺序和相关性排序不一致的问题。

## 修复前

系统为了让上下文更完整，会补全父分段、兄弟分段或原文片段。但补全过程如果改变列表顺序，用户看到的参考资料就不再按相关性排列。

## 修复后

补全只负责补内容，不改变原始召回和重排顺序。最终参考资料继续按相关性输出。

## 产品价值

v04 解决模型吃到什么，v05 解决用户看到什么。引用顺序稳定后，答案可信度和可解释性更强。
