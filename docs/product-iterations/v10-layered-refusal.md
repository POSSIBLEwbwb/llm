# v10：分层拒答兜底

## 版本目标

增加分层拒答能力，减少知识库没有答案时的错误回答。

## 修复前

当召回结果质量很低，或者知识库根本没有答案时，模型仍然可能生成一个看似合理的回答。

## 修复后

从检索、重排、生成三层控制回答边界：

- 检索阶段判断是否有高质量片段。
- 重排阶段用 minScore 过滤弱相关内容。
- 生成阶段要求模型在资料不足时明确拒答。

## 产品价值

v09 让模型更忠实，v10 给系统加安全边界。对企业级问答来说，知道什么时候不能回答，和能回答一样重要。
